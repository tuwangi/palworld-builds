---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span
  class={`inline-flex items-center rounded-full border border-white/10 bg-white/5 py-1 px-2.5 text-xs font-medium text-zinc-300 ${className}`}
>
  <slot />
</span>
