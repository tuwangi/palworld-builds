---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span
  class={`inline-flex items-center rounded-full border border-[var(--line)] bg-[var(--paper-deep)] py-1.5 px-3 text-xs font-semibold text-[var(--ink-soft)] ${className}`}
>
  <slot />
</span>
